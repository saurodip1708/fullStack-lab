// Assignment 4: Node.js Script for the REPL Environment, Including Functions, Array Methods, and Asynchronous Operations
// To use REPL: run `node` in terminal
// Example functions and array methods for REPL:
function square(x) { return x * x; }
const arr = [1, 2, 3, 4];
arr.map(square); // [1, 4, 9, 16]

// Asynchronous operation example
setTimeout(() => console.log('Async operation in REPL!'), 1000);
